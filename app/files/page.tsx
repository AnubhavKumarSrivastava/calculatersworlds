import Link from "next/link";import {tools,categoryPath,type Category} from "@/lib/tools";export const revalidate=3600;export const metadata={title:"File Tools",description:"Compress, resize, convert and organize common image and PDF files."};const other=(["calculators","finance","dev","seo","files"] as Category[]).filter(x=>x!=="files");export default function Page(){const x=Object.entries(tools).filter(([,t])=>t.category==="files");return <section className="container py-14"><div className="tool-hero"><p className="hero-badge text-[#2563eb]">⇩ File Tools</p><h1 className="gradient-text">File Tools</h1><p>Compress, resize, convert and organize common image and PDF files.</p></div><div className="mt-10 grid gap-4 sm:grid-cols-2 lg:grid-cols-3">{x.map(([s,t])=><Link href={categoryPath(t,s)} key={s} className="glass hover-lift rounded-2xl p-6"><span className="text-2xl text-[#2563eb]">⇩</span><h2 className="mt-4 font-bold text-[#172033]">{t.title}</h2><p className="mt-2 text-sm leading-6 text-[#667085]">{t.description}</p><span className="mt-5 block text-sm text-[#2563eb]">Open →</span></Link>)}</div><div className="mt-16 border-t border-[#e5eaf1] pt-10"><h2 className="text-3xl font-black text-[#172033]">Explore other CalcIndia tools</h2><div className="mt-6 grid gap-4 sm:grid-cols-2 lg:grid-cols-4">{other.map(c=><Link href={"/"+c} key={c} className="glass hover-lift rounded-2xl p-5"><b className="text-[#172033]">{c==="calculators"?"🧮 Calculators":c==="finance"?"₹ Finance":c==="dev"?"{ } Developer":c==="seo"?"# SEO":"📁 Files"}</b><p className="mt-2 text-sm text-[#667085]">Explore this complete category.</p></Link>)}</div></div></section>}
