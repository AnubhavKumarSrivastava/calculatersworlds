import type {MetadataRoute} from "next";import {tools,categoryPath} from "@/lib/tools";export default function sitemap():MetadataRoute.Sitemap{const base=process.env.NEXT_PUBLIC_SITE_URL||"https://www.calcindia.example";const category=["/calculators","/finance","/dev","/seo","/files"];return [...category.map(path=>({url:base+path,lastModified:new Date(),changeFrequency:"monthly" as const,priority:.8})),...Object.entries(tools).map(([slug,t])=>({url:base+categoryPath(t,slug),lastModified:new Date(),changeFrequency:"monthly" as const,priority:.7}))]}
